export interface FlowConfig {
  flow_config_id: string;
  agent_id: string;
  provider: string;
  api_key: string
}

export interface FlowConfigData {
  flowConfigId: string;
  agentId: string;
  provider: string;
  apiKey: string
}
